# impostor

Static "Impostor" party game app for GitHub Pages.

## Run locally

Open `index.html` (or `./index.html`) in a browser.

## Deploy on GitHub Pages

- Push this branch to GitHub.
- In repository settings, enable **Pages** and set source to the default branch root.
- The site serves directly from `index.html`.
